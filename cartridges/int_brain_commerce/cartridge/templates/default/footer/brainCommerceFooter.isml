<isif condition="${pdict.loadBrainCommerceSDK}">
    <input type="hidden" class="brainCommerceSDKBaseAPIURL" value="${pdict.baseAPIURL}" />
    <input type="hidden" class="brainCommerceSDKAPIKey" value="${pdict.sdkAPIKey}" />

    <script defer="defer" src="${URLUtils.staticURL('/js/brainCommerceSDK.js')}"></script>
</isif>